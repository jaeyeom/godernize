package main

import (
	"fmt"
	"os"
	"strings"
)

func main() {
	data := strings.TrimSpace("  hello  ")
	fmt.Println(data)

	var err error
	if os.IsNotExist(err) {
		fmt.Println("File does not exist")
	}
}